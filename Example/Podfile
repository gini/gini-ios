project 'Example.xcodeproj'

platform :ios, '10.0'

target 'Example' do
  use_frameworks!
  pod 'Gini', :path => '../', :testspecs => ['Tests']
  pod 'Gini/Documents', :path => '../'
  pod 'Gini/Pinning', :path => '../'
end
